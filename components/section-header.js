export function SectionHeader({ label }) {
  return (
    <h2 className="mb-8 w-full text-center font-mono text-[12px] font-medium uppercase tracking-[0.12em] text-[#737373]">
      {label}
    </h2>
  );
}
